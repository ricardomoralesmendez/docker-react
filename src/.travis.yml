language: generic
sudo: required
services: 
  - docker

before_install:
  - docker build -t rickmorales/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true rickmorales/docker-react npm run test